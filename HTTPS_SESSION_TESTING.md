# Инструкция по тестированию Session Management с HTTPS

## Цель тестирования

Проверить, что Session Management правильно работает для HTTPS запросов:
- ✅ Создание сессии для первого HTTPS запроса
- ✅ Переиспользование сессии для последующих запросов к тому же серверу
- ✅ Сохранение TCP соединения между запросами
- ✅ Корректная обработка TLS handshake через несколько раундов

## Подготовка

### 1. Получите токен Яндекс Диск

Если у вас еще нет токена:
```powershell
# Следуйте инструкциям в listeners/yandex-disk/GETTING_TOKEN.md
```

### 2. Убедитесь, что Deno установлен

```powershell
deno --version
```

## Запуск системы

### Шаг 1: Запуск LISTENER

Откройте **первый терминал PowerShell**:

```powershell
cd listeners\yandex-disk
$env:YANDEX_DISK_TOKEN="ваш_токен"
$env:REQUESTS_FOLDER=".mysogatone/requests"
$env:RESPONSES_FOLDER=".mysogatone/responses"
$env:POLL_INTERVAL_MS="2000"
$env:CONNECTION_TIMEOUT_MS="60000"

deno task start
```

**Ожидаемый вывод:**
```
🚀 LISTENER для Яндекс Диск запускается...
📁 Папка запросов: .mysogatone/requests
📁 Папка ответов: .mysogatone/responses
⏱️  Интервал polling: 2000ms
⏱️  Таймаут подключения: 60000ms
🔍 Мониторинг папки .mysogatone/requests...
```

### Шаг 2: Запуск CALLER

Откройте **второй терминал PowerShell**:

```powershell
cd callers\deno
$env:YANDEX_DISK_TOKEN="ваш_токен"
$env:SOCKS5_PORT="1080"
$env:REQUESTS_FOLDER=".mysogatone/requests"
$env:RESPONSES_FOLDER=".mysogatone/responses"
$env:POLL_INTERVAL_MS="2000"
$env:RESPONSE_TIMEOUT_MS="60000"

deno task start
```

**Ожидаемый вывод:**
```
Используется YandexDiskProvider для ConnectionHandler.
🚀 SOCKS5 сервер запущен на порту 1080
```

## Тестирование HTTPS запросов

### Тест 1: Первый HTTPS запрос (создание сессии)

В **третьем терминале PowerShell** выполните:

```powershell
curl.exe --proxy socks5://127.0.0.1:1080 https://ya.ru
```

**Что проверить в логах CALLER:**
```
[YandexDiskConnectionHandler] Создание соединения для ya.ru:443
[YandexDiskConnectionHandler] Сессия: <session-id>, первый в сессии: true, keep-alive: true
[SessionManager] Создана сессия <session-id> для ya.ru:443
```

**Что проверить в логах LISTENER:**
```
[<request-id>] 🔗 Сессия: <session-id>, первый в сессии: true, keep-alive: true
[SessionManager] Создание новой сессии <session-id> для ya.ru:443
[SessionManager] Сессия <session-id> создана, TCP соединение установлено
[<request-id>] ✅ TCP соединение получено (сессия: <session-id>, переиспользовано: false)
```

### Тест 2: Второй HTTPS запрос (переиспользование сессии)

**Важно:** Выполните второй запрос **в течение 60 секунд** после первого, чтобы сессия не была очищена.

```powershell
curl.exe --proxy socks5://127.0.0.1:1080 https://ya.ru
```

**Что проверить в логах CALLER:**
```
[YandexDiskConnectionHandler] Создание соединения для ya.ru:443
[YandexDiskConnectionHandler] Сессия: <session-id>, первый в сессии: false, keep-alive: true
```

**Важно:** `session-id` должен быть **таким же**, как в первом запросе!

**Что проверить в логах LISTENER:**
```
[<request-id-2>] 🔗 Сессия: <session-id>, первый в сессии: false, keep-alive: true
[SessionManager] Сессия <session-id> уже существует, переиспользуем
[<request-id-2>] ✅ TCP соединение получено (сессия: <session-id>, переиспользовано: true)
```

**Ключевой момент:** Должно быть `переиспользовано: true`!

### Тест 3: HTTPS запрос к другому серверу (новая сессия)

```powershell
curl.exe --proxy socks5://127.0.0.1:1080 https://google.com
```

**Что проверить:**
- Должна быть создана **новая сессия** с другим `session-id`
- В логах должно быть `первый в сессии: true`

### Тест 4: HTTP запрос (ephemeral сессия)

```powershell
curl.exe --proxy socks5://127.0.0.1:1080 http://httpbin.org/ip
```

**Что проверить в логах CALLER:**
```
[YandexDiskConnectionHandler] Создание соединения для httpbin.org:80
[YandexDiskConnectionHandler] Сессия: <ephemeral-session-id>, первый в сессии: true, keep-alive: false
```

**Важно:** Для HTTP (порт 80) должна создаваться ephemeral сессия, которая не переиспользуется.

### Тест 5: Проверка статистики сессий

В логах CALLER и LISTENER периодически (каждые 60 секунд) должна выполняться очистка неактивных сессий:

```
[SessionManager] Очищено X неактивных сессий (idle > 60000ms)
```

## Что проверить

### ✅ Успешные признаки

1. **Создание сессии для HTTPS:**
   - В CALLER: `[SessionManager] Создана сессия ... для ya.ru:443`
   - В LISTENER: `[SessionManager] Создание новой сессии ... для ya.ru:443`

2. **Переиспользование сессии:**
   - В CALLER: `первый в сессии: false` для второго запроса к тому же серверу
   - В LISTENER: `переиспользовано: true`

3. **Сохранение TCP соединения:**
   - В LISTENER: `Сессия ... уже существует, переиспользуем`
   - TCP соединение не закрывается между запросами

4. **Ephemeral сессии для HTTP:**
   - Для HTTP (порт 80) создаются ephemeral сессии
   - Каждый HTTP запрос получает новый session-id

### ❌ Проблемы, на которые обратить внимание

1. **Сессия не переиспользуется:**
   - Если второй запрос к тому же серверу создает новую сессию вместо переиспользования
   - Проверьте логи: должно быть `первый в сессии: false`

2. **TCP соединение закрывается:**
   - Если в LISTENER видите `Соединение закрыто` после каждого запроса для HTTPS
   - Проверьте, что `keepSessionAlive: true` передается правильно

3. **Ошибки TLS handshake:**
   - Если HTTPS запросы падают с ошибками TLS
   - Проверьте, что TCP соединение действительно переиспользуется

## Отладка

### Просмотр активных сессий

В коде можно добавить логирование статистики сессий:

```typescript
// В CALLER (main.ts) можно добавить:
setInterval(() => {
  const stats = sessionManager.getStats();
  console.log('[SessionManager] Статистика:', JSON.stringify(stats, null, 2));
}, 30000); // каждые 30 секунд
```

### Проверка метаданных запроса

Метаданные запроса содержат информацию о сессии:
- `sessionId` - идентификатор сессии
- `isFirstInSession` - первый ли это запрос в сессии
- `keepSessionAlive` - нужно ли сохранять сессию

Можно проверить файлы `.req` в Яндекс Диске через API или веб-интерфейс.

## Ожидаемое поведение

### HTTPS запросы (порт 443)

1. **Первый запрос:**
   - Создается новая сессия
   - `isFirstInSession: true`
   - `keepSessionAlive: true`
   - TCP соединение создается в LISTENER

2. **Последующие запросы к тому же серверу (в течение 60 секунд):**
   - Переиспользуется существующая сессия
   - `isFirstInSession: false`
   - `keepSessionAlive: true`
   - TCP соединение переиспользуется в LISTENER

3. **После 60 секунд неактивности:**
   - Сессия очищается
   - Следующий запрос создаст новую сессию

### HTTP запросы (порт 80)

- Каждый запрос получает ephemeral сессию
- `isFirstInSession: true` всегда
- `keepSessionAlive: false`
- TCP соединение закрывается после каждого запроса

## Следующие шаги

После успешного тестирования:
1. ✅ Убедитесь, что все тесты проходят
2. ✅ Задокументируйте результаты
3. ✅ Создайте автоматические тесты для Session Management
